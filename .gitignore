# Build output — every app here carries its own target/
target/
**/target/
dist/
build/

# macOS
.DS_Store
**/.DS_Store

# Xcode / Gradle local state
**/xcuserdata/
**/.gradle/
**/local.properties

# Compiled native libs — build output from `rsc build --android`, not source.
# A 23MB libshowcase.so was committed before this file existed.
**/jniLibs/
*.so
*.dylib
*.a
